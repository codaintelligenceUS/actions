{
  "name": "regexp-from-string",
  "version": "0.1.0",
  "description": "Creates RegExp from a string, e.g. `/string/gi` and provides some utils to work with RegExp as strings, for example, to extract multi RegExps defined within a same string, e.g. `/Rock/, /and/, /roll/i`.",
  "main": "lib/regexp-from-string.js",
  "scripts": {
    "test": "mocha ./tests/main.test.js",
    "test:all": "mocha ./tests -b"
  },
  "keywords": [
    "regexp",
    "strings",
    "regex",
    "regexes",
    "regexps",
    "multiple",
    "Regular Expression",
    "Regular Expressions"
  ],
  "author": "Diego ZoracKy",
  "license": "MIT",
  "devDependencies": {
    "chai": "^3.5.0",
    "mocha": "^3.3.0"
  }
}
